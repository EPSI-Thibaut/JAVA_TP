# Nom de ma base de données
spring.application.name=petstore

# Mes informations de connexion à la base de données
spring.datasource.url=jdbc:mariadb://localhost:3306/petstore
spring.datasource.username=thibaut
spring.datasource.password=abierto
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
